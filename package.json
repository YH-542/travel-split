{
  "name": "travel-split",
  "version": "1.0.0",
  "private": true,
  "description": "Travel Split - グループ旅行の割り勘精算アプリ",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && node --watch src/server.js",
    "dev:frontend": "cd frontend && npx vite --host 0.0.0.0",
    "build": "cd frontend && npm run build",
    "start": "cd backend && cross-env NODE_ENV=production node src/server.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3"
  }
}
